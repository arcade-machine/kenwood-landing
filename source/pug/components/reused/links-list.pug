mixin links-list(array, subclass)

    ul.links-list(class=subclass)
        each link,index in array
            if index === 0
                li.links-list__item
                    span.links-list__link.links-list__link--active= link
            else
                li.links-list__item
                    a.links-list__link(href="#")= link