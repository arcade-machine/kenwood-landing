include components/abstract/mixins

doctype html
html(lang='ru')
    +head()

    body
        .page-layout
            +header

            main
                +top
                +category
                +promo
                +features
                +conditions
                +info
                +actions

        script(src=js + "main.js")
